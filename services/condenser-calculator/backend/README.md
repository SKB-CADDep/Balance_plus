```markdown
# Condenser Calculator (Backend)

Локальный backend сервиса расчёта конденсатора на базе **FastAPI** и **SQLAlchemy**.

## 📌 Что делает эта папка?
Здесь находится ядро веб-сервиса: REST API для приема параметров от клиента, бизнес-логика маршрутизации, взаимодействие с базой данных (PostgreSQL) для хранения справочников (конденсаторы, материалы) и запуск изолированного математического ядра для расчетов.

## ⚙️ Настройка окружения (.env)
Для локального запуска необходимо задать переменные окружения. Создайте файл `.env` в корне директории `backend/` (рядом с этим README) на основе следующего примера:

```env
# Пример .env файла
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/condenser_db
SECRET_KEY=your_super_secret_key_here
DEBUG=True
```

## 🐳 Запуск через Docker (Инфраструктура)
Для работы локального бэкенда требуется база данных и брокер сообщений. Запустите базовую инфраструктуру в фоновом режиме:

```bash
docker-compose up -d db redis
```

> **💡 Важно:** Эта команда приведена в качестве примера и сработает только в том случае, если в вашем корневом конфигурационном файле `docker-compose.yml` эти сервисы названы именно `db` (PostgreSQL) и `redis`. Если используются другие имена (например, `postgres`), команду необходимо скорректировать.

## 🚀 Установка и локальный запуск (Poetry)
Мы используем пакетный менеджер **Poetry**. Все команды выполняются из директории `services/condenser-calculator/backend`.

**1. Установите зависимости:**
```bash
poetry install
```

**2. Примените миграции базы данных (Alembic):**  
Эта команда создаст нужные таблицы в вашей базе данных на основе ORM-моделей.
```bash
poetry run alembic upgrade head
```

**3. Запустите сервер:**
```bash
poetry run uvicorn app.main:app --reload
```

После запуска интерактивная документация к API (Swagger) будет доступна по адресу: 👉 **http://localhost:8000/docs**

## 🌐 Маршрутизация и порты (Справка по проекту)

> **⚠️ Примечание по портам:**  
> В таблице ниже представлены порты микросервисов для локальной разработки. Учитывайте, что ссылки вроде `localhost:5252` специфичны исключительно для сервиса **Valve Stems** (Штоки клапанов). Текущий сервис (Condenser Calculator) стандартно использует порт `8000`. Всегда сверяйтесь с вашим локальным `.env`.

| Сервис / Модуль | Окружение | URL (Локально) |
| :--- | :--- | :--- |
| **Condenser Calculator** | Backend (API) | `http://localhost:8000` |
| **Condenser Calculator** | Frontend (UI) | `http://localhost:5173` |
| **Valve Stems** | Backend (API) | `http://localhost:5252` |
| **Infrastructure** | PostgreSQL | `localhost:5432` |
| **Infrastructure** | Redis | `localhost:6379` |

## 🧪 Запуск тестов
Для проверки работоспособности сервиса выполните команду:

```bash
poetry run pytest
```
```
