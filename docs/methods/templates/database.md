# [DB-ID] Название сущности (оборудования)

**Версия схемы:** 1.0
**Статус:** Черновик / Активно
**Связанные методики:** [ID-METOD-1], [ID-METOD-2]

## 1. Описание сущности
Что описывает эта база данных. Откуда берутся данные (чертежи, формуляры, паспорта).

## 2. Структура данных (Schema)

### 2.1. Группа параметров (например, Геометрия)
| Ключ (JSON/Code) | Название (UI) | Ед. изм. (DB) | Тип данных | Обяз. | Описание / Источник | Валидация |
| :--- | :--- | :---: | :---: | :---: | :--- | :--- |
| `key_name` | Название для пользователя | мм | Float | Да | Откуда брать значение | min > 0 |

### 2.2. Группа параметров (например, Режимы)
...

## 3. Пример JSON (Payload)
Как выглядит объект данных при передаче в расчетный модуль.

```json
{
  "group": {
    "key": value
  }
}
```

## 4. Особые указания по заполнению
Нюансы, которые не влезли в таблицу (например, как считать ходы воды для сложных схем).
