

# Спецификация продукта «Модуль расчёта штоков клапанов (WSA Valve Stems)» v2.4

---

## Changelog

> **v2.2** — Базовая спецификация (Legacy-замена, single-valve)
> **v2.3** — Групповой ввод параметров, multi-valve, группировка по геометрии
> **v2.4** — Секции турбоустановки, глобальные параметры воздуха/вакуума, убран индивидуальный режим, dashboard секций, auto-numbering секций

### Дельта v2.3 → v2.4

| # | Изменение | Детали |
|---|-----------|--------|
| 1 | **Добавлены глобальные параметры** | `P_air` (default 1.033), `T_air` (default 27), `P_lst_leak_off` (default 0.97) |
| 2 | **Удалён индивидуальный режим** | BR-13, BR-15, UC-04 alt — удалены. Внутри группы все клапаны всегда идентичны |
| 3 | **Добавлена концепция «Секция установки»** | ЦВД, ЦСД, ЦНД, контур ВД, контур СД, контур НД — 6 фиксированных типов, auto-numbering |
| 4 | **Dashboard секций** | Новый экран при входе через Task Manager — статус всех секций проекта |
| 5 | **Обновлена структура GitLab** | `valve-stems/{sectionId}/result.json` + `manifest.json` |
| 6 | **Обновлена логика гидрации** | Двухэтапная: manifest → dashboard → result.json конкретной секции |
| 7 | **Уточнена формула кол-ва полей отсосов** | `count_parts - 2` (последний отсос = глобальный `P_lst_leak_off`) |

---

## Раздел 1. Общая информация

| Поле | Значение |
|------|----------|
| Название продукта | Расчётный модуль «Утечки через штоки клапанов» (Valve Stems) |
| Заказчик | Отдел инженерных расчётов (БТР, БПР, БВП) / Деминов А.М. |
| Аналитик | Шлёгин Л.Р., Гаврилов П.Я. |
| Дата передачи в разработку | 15.12.2025 |
| Приоритет | Высокий (Замена Legacy ПО) |
| Целевой срок Demo | Q2 2026 |
| **Версия** | **2.4** (Секции установки, глобальные параметры) |

---

## Раздел 2. Проблема и цель

> **Проблема:**
> Текущий процесс расчёта штоков клапанов фрагментирован и выполняется в проприетарном/устаревшем ПО (SiTh, DOS).
> - Отсутствует версионирование расчётов.
> - Нет централизованного хранения данных (геометрия, результаты).
> - Отсутствует БД геометрии.
> - Разрыв контекста: ручной перенос данных (PDF-чертежи → txt-исходники → Excel-результаты → Word-отчёты).
> - Высокая трудоёмкость подготовки данных.
> - **Нет связки «часть установки (ЦВД/ЦСД/ЦНД) → набор клапанов → результат»** — расчёты для разных секций турбоустановки ведутся разрозненно.
>
> **Цель:**
> Реализовать принцип «Единого окна» (Single Window) для расчёта утечек пара через штоки:
> 1. Создать единую БД геометрии клапанов.
> 2. Обеспечить связку «Проект — **Секция установки** — Геометрия — Результат».
> 3. Исключить использование сторонних файлов (txt/excel) в процессе расчёта.
> 4. Автоматизировать определение расходов отбора/подвода пара.
> 5. **Обеспечить независимый расчёт и хранение результатов для каждой секции (части) турбоустановки.**
>
> **Критерий успеха:**
> - Точность расчёта: отклонение от текущих методик < 0.1%.
> - Время выполнения расчёта: < 0.2 сек.
> - Бесшовная передача данных: БД геометрии → Расчёт → БД результатов.
> - Надёжная связка «Проект — Секция — Результат».

---

## Раздел 3. Пользователи и роли

| Роль | Кто это | Что делает в системе |
|------|---------|---------------------|
| Инженер-расчётчик | Сотрудники бюро (БТР, БПР, БВП) | Создаёт секции установки, выбирает геометрию (клапаны), вводит/корректирует термодинамические параметры, выполняет расчёт, анализирует результаты, сохраняет их в GitLab или Excel. |

---

## Раздел 4. Функциональные требования (Use Cases)

### UC-00 (новый): Управление секциями установки

| Поле | Описание |
|------|----------|
| Актор | Инженер-расчётчик |
| Предусловие | Модуль открыт в контексте проекта (через Task Manager или автономно). |
| Основной сценарий | 1. Система отображает **Dashboard секций** — список всех секций проекта с их статусом.<br>2. Для каждой секции отображается: название, статус (рассчитана / не рассчитана), дата последнего расчёта, автор, краткая сводка (P, T, кол-во клапанов, суммарный расход).<br>3. Пользователь может:<br>   - **Открыть** существующую секцию → переход к UC-01/UC-02 с гидрацией данных.<br>   - **Пересчитать** секцию → переход к шагу ввода параметров с предзаполненными полями.<br>   - **Создать новую секцию** (кнопка «+ Добавить секцию»):<br>     a. Выбирает тип из фиксированного списка (BR-20).<br>     b. Система присваивает имя с auto-numbering (BR-21).<br>     c. Переход к UC-03 (выбор клапанов). |
| Результат | Пользователь находится в контексте конкретной секции, готов к расчёту. |

### UC-01 (обновлён): Расчёт в контексте задачи (Task Manager)

| Поле | Описание |
|------|----------|
| Актор | Инженер-расчётчик |
| Предусловие | Задача выбрана в Task Manager. Модуль открыт в Iframe. |
| Основной сценарий | 1. Система получает `projectId` из Task Manager (postMessage).<br>2. Система запрашивает `manifest.json` из GitLab-репозитория проекта.<br>3. **Dashboard секций (UC-00):**<br>   - Отображаются все секции с их статусом.<br>   - Пользователь выбирает секцию или создаёт новую.<br>4. **Если секция уже рассчитана (Гидрация):**<br>   - Загружается `result.json` из директории секции.<br>   - Автоматически выбираются клапаны по ID.<br>   - Подгружается геометрия из БД.<br>   - Клапаны группируются по типу геометрии (BR-12).<br>   - Поля термодинамики заполняются сохранёнными значениями.<br>5. **Если секция новая:**<br>   - Пользователь ищет и выбирает клапаны (UC-03).<br>6. Пользователь заполняет/корректирует глобальные параметры (свежий пар, воздух, вакуум).<br>7. Пользователь заполняет давления промежуточных отсосов для каждой группы клапанов (UC-04).<br>8. Пользователь нажимает «Рассчитать».<br>9. Система выполняет расчёт для каждой группы (один расчёт × количество клапанов).<br>10. Система отображает результаты: детализация по группам + сводные таблицы СК/РК.<br>11. Пользователь нажимает «Отправить результаты в GitLab». |
| Результат | Файл `result.json` сохранён в `valve-stems/{sectionId}/result.json`. Файл `manifest.json` обновлён. |

### UC-02 (обновлён): Автономный расчёт (Direct Access)

| Поле | Описание |
|------|----------|
| Актор | Инженер-расчётчик |
| Предусловие | Пользователь авторизован, модуль открыт по прямой ссылке (вне контекста Task Manager). |
| Основной сценарий | 1. Пользователь выбирает тип секции из фиксированного списка (для организации отчёта).<br>2. Пользователь ищет и выбирает клапаны (UC-03).<br>3. Система автоматически подгружает геометрию и группирует клапаны (BR-12).<br>4. Пользователь вводит глобальные параметры и давления промежуточных отсосов.<br>5. Нажимает «Рассчитать».<br>6. Просматривает результаты.<br>7. Нажимает «Сохранить в Excel».<br>**Примечание:** Если геометрия модифицирована (BR-11), расчёт сохраняется **только** в Excel. |
| Результат | Сформирован и скачан `.xlsx` файл с отчётом. Лист назван по имени секции. |

### UC-03: Поиск и выбор оборудования

| Поле | Описание |
|------|----------|
| Актор | Инженер-расчётчик |
| Предусловие | Секция установки выбрана/создана. |
| Основной сценарий | 1. Пользователь переходит на вкладку поиска.<br>2. Ищет клапаны (по отдельности Стопорные (СК) и Регулирующие (РК)).<br>3. При выборе одного типа (например, СК), система предлагает связанные варианты другого типа (РК), если связи заданы в БД.<br>4. Пользователь подтверждает выбор конфигурации.<br>5. Система группирует выбранные клапаны по ключу геометрии (BR-12). |

### UC-04 (обновлён): Ввод параметров отсосов по группам

| Поле | Описание |
|------|----------|
| Актор | Инженер-расчётчик |
| Предусловие | Клапаны выбраны и сгруппированы (BR-12). Экран ввода данных открыт. |
| Основной сценарий | 1. Для каждой группы клапанов система отображает секцию с заголовком: `{Тип} ({Кол-во} шт) — {N} участков, {N-1} отсосов`.<br>2. Система отображает `N-2` полей ввода **промежуточных** давлений отсосов (последний отсос = глобальный `P_lst_leak_off`).<br>3. Если `N = 2` (единственный отсос = последний), полей ввода нет — отображается информационная подпись: «Единственный отсос = P_lst_leak_off».<br>4. Пользователь заполняет поля. Введённые значения применяются **ко всем** клапанам группы.<br>5. Расчёт для группы: один вызов расчётного ядра, результат × количество клапанов. |
| Результат | Все поля отсосов заполнены. Форма готова к расчёту. |

---

## Раздел 5. Бизнес-правила

| ID | Правило | Описание / Логика |
|----|---------|-------------------|
| BR-01 | **Авто-топология** | Количество участков определяется автоматически исходя из геометрии (скрыто от пользователя). Количество отсосов = `count_parts - 1`. |
| BR-02 | **Распределение давлений** | Параметры перед ($P_0$) и за ($P_2$) участком определяются автоматически (см. Таблицу 5.1). Последний отсос = глобальный `P_lst_leak_off`. Давление воздуха = глобальный `P_air`. |
| BR-03 | **Единицы измерения (Input)** | В полях ввода — выпадающие списки (Dropdown) для выбора единиц.<br>**Defaults:** Давление: `кгс/см²`, Температура: `°С`, Энтальпия: `ккал/кг` |
| BR-04 | **Единицы измерения (Output)** | Результаты всегда в фиксированных единицах: Давление: `кгс/см²`, Температура: `°С`, Энтальпия: `ккал/кг`, Расход: `т/ч` |
| BR-05 | **Агрегация результатов** | Сводные таблицы формируются по типу клапана (`type`):<br>1. **Σ СК** = сумма расходов всех клапанов с `type = 'СК'` (по всем группам геометрии).<br>2. **Σ РК** = сумма расходов всех клапанов с `type = 'РК'` (по всем группам геометрии).<br>Внутри сводной таблицы — разбивка по группам. |
| BR-06 | **Округление (Display)** | Только для отображения на UI: Давление: 4 значащих цифры, Температура: до 0.1, Энтальпия: до 0.1, Расход: до 0.01 |
| BR-07 | **Валидация ошибок** | Поле не заполнено → «Поле обязательно». Не число → «Введите число». $P_2 > P_1$ → «Давление за участком выше начального». |
| BR-08 | **Методика расчёта расхода** | Расчёт по формулам истечения пара через кольцевую щель (лабиринтное уплотнение). Итерационный метод для определения критического давления. Ссылка: [CALC-STEM-RTM](https://github.com/LevShlyogin/Balance_plus/blob/main/docs/methods/balance/CALC-STEM-RTM.md). |
| BR-09 | **Свойства рабочего тела** | Плотность и энтальпия пара — по IAPWS-97 на основе $P$ и $H_{const}$ (дросселирование, энтальпия постоянна). |
| BR-10 | **Мнемосхема клапана** | ~~Реализовать в v1.~~ **Перенесено в v2 (следующий релиз).** |
| BR-11 | **Ручной ввод геометрии** | Пользователь может разблокировать поля геометрии из БД для кастомных расчётов. Такие поля подсвечиваются «Modified». Расчёт с модификацией сохраняется **только в Excel**. |
| BR-12 | **Группировка клапанов** | Выбранные клапаны автоматически группируются по **ключу геометрии**: `(diameter, clearance, count_parts, len_part1, ..., len_partN, round_radius)`. Клапаны с идентичным ключом попадают в одну группу. Внутри группы сортировка по `type` (СК → РК). |
| BR-14 | **Оптимизация расчёта** | Всегда один расчёт на группу. Расход одного клапана × количество = расход группы. |
| BR-16 | **Секция установки** | Расчёт выполняется в контексте **секции** (части турбоустановки). Секция определяет: набор клапанов, параметры свежего пара, директорию хранения результатов. |
| BR-17 | **Независимость секций** | Секции рассчитываются и сохраняются **независимо**. Пересчёт одной секции не затрагивает результаты других. |
| BR-18 | **Dashboard секций** | При входе через Task Manager система отображает статус всех секций проекта: рассчитана / не рассчитана, дата, автор, краткая сводка. |
| BR-19 | **Manifest** | Файл `manifest.json` в корне директории `valve-stems/` хранит метаданные всех секций проекта и обновляется при каждом сохранении. |
| BR-20 | **Фиксированный список типов секций** | Доступно **6 типов** секций: `ЦВД`, `ЦСД`, `ЦНД`, `Контур ВД`, `Контур СД`, `Контур НД`. Список фиксирован для всех турбин. Пользователь выбирает из этого списка при создании секции. |
| BR-21 | **Auto-numbering секций** | Если секция данного типа добавляется **более одного раза**, система автоматически присваивает суффикс-номер:<br>— Первый ЦСД → `ЦСД-1`<br>— Второй ЦСД → `ЦСД-2`<br>— Единственный ЦВД → `ЦВД` (без номера, или `ЦВД-1` — на усмотрение UX).<br>**Правило:** при добавлении второй секции того же типа первая секция **ретроактивно переименовывается** с суффиксом `-1`. |
| BR-22 | **Значения по умолчанию глобальных параметров** | `P_air` = 1.033 кгс/см² (редактируемое), `T_air` = 27°С (диапазон 0–100°С, редактируемое), `P_lst_leak_off` = 0.97 кгс/см² (редактируемое, всегда < `P_air`). |
| BR-23 | **Последний отсос — глобальный** | Давление последнего отсоса (`P_lst_leak_off`) одинаково для **всех** клапанов **всех** групп внутри одной секции. Оно всегда подключено к вакуумной установке. Если у клапана всего 1 отсос — это именно `P_lst_leak_off`. |

### Таблица 5.1. Логика распределения давлений (BR-02, обновлено)

| Участков (`count_parts`) | Отсосов | Промежуточных полей ввода | Схема давлений (P₀ → P₂) |
|:---:|:---:|:---:|:---|
| **2** | 1 | **0** | 1. `P_fresh` → **`P_lst_leak_off`**<br>2. `P_air` → **`P_lst_leak_off`** |
| **3** | 2 | **1** | 1. `P_fresh` → `P_leak_off_1`<br>2. `P_leak_off_1` → **`P_lst_leak_off`**<br>3. `P_air` → **`P_lst_leak_off`** |
| **4** | 3 | **2** | 1. `P_fresh` → `P_leak_off_1`<br>2. `P_leak_off_1` → `P_leak_off_2`<br>3. `P_leak_off_2` → **`P_lst_leak_off`**<br>4. `P_air` → **`P_lst_leak_off`** |
| **5** | 4 | **3** | 1. `P_fresh` → `P_leak_off_1`<br>2. `P_leak_off_1` → `P_leak_off_2`<br>3. `P_leak_off_2` → `P_leak_off_3`<br>4. `P_leak_off_3` → **`P_lst_leak_off`**<br>5. `P_air` → **`P_lst_leak_off`** |

> **Формула:** количество полей ввода на группу = `count_parts - 2`

---

## Раздел 6. Данные

### 6.1. Входные данные

| Название | Код поля | Ед. изм. (Default) | Тип ввода | Уровень | Default | Примечание |
|----------|----------|---------------------|-----------|---------|---------|------------|
| Давление свежего пара | `P_fresh` | кгс/см² | Single | **Глобальный** | — | |
| Температура свежего пара | `T_fresh` | °С | Single | **Глобальный** | — | Взаимоисключающее с H |
| Энтальпия свежего пара | `H_fresh` | ккал/кг | Single | **Глобальный** | — | Взаимоисключающее с T |
| Давление воздуха | `P_air` | кгс/см² | Single | **Глобальный** | **1.033** | Барометрическое, редактируемое |
| Температура воздуха | `T_air` | °С | Single | **Глобальный** | **27** | Диапазон 0–100°С |
| Давление последнего отсоса | `P_lst_leak_off` | кгс/см² | Single | **Глобальный** | **0.97** | Вакуум (< P_air), одно на всю секцию |
| Давления промежуточных отсосов | `P_leak_offs` | кгс/см² | Dynamic Array | **Группа** | — | Кол-во полей = `count_parts - 2`. Если `count_parts = 2` → полей нет |

### 6.2. Выходные данные

| Данные | Потребитель | Формат | Состав |
|--------|-------------|--------|--------|
| Результат расчёта (Git) | GitLab Repo | `result.json` (per section) | Секция, ID клапанов, глобальные параметры, группы, рассчитанные расходы |
| Манифест (Git) | GitLab Repo | `manifest.json` | Метаданные всех секций проекта |
| Отчёт (Скачивание) | Пользователь | `.xlsx` | Человекочитаемая таблица, лист по имени секции |
| UI Display | Интерфейс | HTML | Dashboard секций, таблицы по каждой группе + сводные (СК/РК) |

### 6.3. Концептуальная модель данных

**manifest.json** (один на проект):

```json
{
  "projectId": "prj-123",
  "turbineId": "turb-456",
  "turbineName": "К-800-240-5",
  "sections": [
    {
      "sectionId": "CVD",
      "sectionType": "ЦВД",
      "label": "ЦВД",
      "status": "calculated",
      "lastUpdated": "2026-01-15T14:30:00Z",
      "calculatedBy": "ivanov",
      "summary": {
        "pFresh": 240,
        "tFresh": 540,
        "valveCount": 5,
        "totalFlow": 1.07
      }
    },
    {
      "sectionId": "CSD-1",
      "sectionType": "ЦСД",
      "label": "ЦСД-1",
      "status": "calculated",
      "lastUpdated": "2026-01-16T09:00:00Z",
      "calculatedBy": "petrov",
      "summary": {
        "pFresh": 38,
        "tFresh": 540,
        "valveCount": 6,
        "totalFlow": 0.85
      }
    },
    {
      "sectionId": "CSD-2",
      "sectionType": "ЦСД",
      "label": "ЦСД-2",
      "status": "empty",
      "lastUpdated": null,
      "calculatedBy": null,
      "summary": null
    }
  ]
}
```

**result.json** (один на секцию):

```json
{
  "meta": {
    "projectId": "prj-123",
    "turbineId": "turb-456",
    "sectionId": "CVD",
    "sectionType": "ЦВД",
    "sectionLabel": "ЦВД",
    "calculatedAt": "2026-01-15T14:30:00Z",
    "calculatedBy": "ivanov"
  },
  "globalInput": {
    "freshSteam": {
      "pressure": { "value": 240, "unit": "kgf/cm2" },
      "temperature": { "value": 540, "unit": "C" },
      "enthalpy": null
    },
    "air": {
      "pressure": { "value": 1.033, "unit": "kgf/cm2" },
      "temperature": { "value": 27, "unit": "C" }
    },
    "lastLeakOff": {
      "pressure": { "value": 0.97, "unit": "kgf/cm2" }
    }
  },
  "valveGroups": [
    {
      "groupId": "group-1",
      "groupLabel": "СК (1 шт) — 3 участка, 2 отсоса",
      "valveType": "STOP_VALVE",
      "geometryKey": {
        "diameter": 100,
        "clearance": 0.25,
        "countParts": 3,
        "lenParts": [313.5, 50, 97.5],
        "roundRadius": null
      },
      "valves": [
        { "id": "uuid-valve-1", "designation": "СК-1 ЦВД" }
      ],
      "intermediateLeakOffs": [10],
      "results": {
        "perValve": {
          "flowTotal": 0.6136,
          "segmentsData": [
            { "pStart": 240, "pEnd": 10, "flow": 0.5366 },
            { "pStart": 10, "pEnd": 0.97, "flow": 0.0740 },
            { "pStart": 1.033, "pEnd": 0.97, "flow": 0.0035 }
          ]
        },
        "groupTotal": {
          "flowTotal": 0.6136,
          "count": 1
        }
      }
    },
    {
      "groupId": "group-2",
      "groupLabel": "РК Тип-1 (3 шт) — 2 участка, 1 отсос",
      "valveType": "CONTROL_VALVE",
      "geometryKey": {
        "diameter": 80,
        "clearance": 0.20,
        "countParts": 2,
        "lenParts": [200, 150],
        "roundRadius": null
      },
      "valves": [
        { "id": "uuid-valve-2", "designation": "РК-1 ЦВД" },
        { "id": "uuid-valve-3", "designation": "РК-2 ЦВД" },
        { "id": "uuid-valve-4", "designation": "РК-3 ЦВД" }
      ],
      "intermediateLeakOffs": [],
      "results": {
        "perValve": {
          "flowTotal": 0.120,
          "segmentsData": [
            { "pStart": 240, "pEnd": 0.97, "flow": 0.110 },
            { "pStart": 1.033, "pEnd": 0.97, "flow": 0.010 }
          ]
        },
        "groupTotal": {
          "flowTotal": 0.360,
          "count": 3
        }
      }
    }
  ],
  "summary": {
    "totalSK": { "flowTotal": 0.6136, "valveCount": 1 },
    "totalRK": { "flowTotal": 0.360, "valveCount": 3 }
  }
}
```

### 6.4. Структура хранения в GitLab

```
{project-repo}/
└── calculations/
    └── valve-stems/
        ├── manifest.json
        ├── CVD/
        │   └── result.json
        ├── CSD-1/
        │   └── result.json
        ├── CSD-2/
        │   └── result.json
        ├── CND-1/
        │   └── result.json
        └── CND-2/
            └── result.json
```

### 6.5. Справочник типов секций (BR-20)

| Код | Отображаемое название | Типичный диапазон P_fresh | Примечание |
|-----|----------------------|---------------------------|------------|
| `CVD` | ЦВД | 130–240 кгс/см² | Цилиндр высокого давления |
| `CSD` | ЦСД | 25–40 кгс/см² | Цилиндр среднего давления |
| `CND` | ЦНД | 3–10 кгс/см² | Цилиндр низкого давления |
| `KVD` | Контур ВД | — | Контур высокого давления |
| `KSD` | Контур СД | — | Контур среднего давления |
| `KND` | Контур НД | — | Контур низкого давления |

---

## Раздел 7. Прототип интерфейса

Приложение состоит из **4-х логических экранов** (шагов):

### Экран 0: Dashboard секций

```
┌──────────────────────────────────────────────────────────────┐
│  ═══ РАСЧЁТ ШТОКОВ КЛАПАНОВ ═══                              │
│  Проект: Заказ №12345 | Турбина: К-800-240-5                │
│                                                              │
│  ┌─── Секции установки ────────────────────────────────────┐ │
│  │                                                          │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │ ЦВД                                  ✅ 15.01.26 │    │ │
│  │  │ P=240, T=540 | 1 СК + 3 РК | Σ = 1.07 т/ч      │    │ │
│  │  │                          [Открыть]  [Пересчитать]│    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                                                          │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │ ЦСД-1                                ✅ 16.01.26 │    │ │
│  │  │ P=38, T=540  | 2 СК + 4 РК | Σ = 0.85 т/ч      │    │ │
│  │  │                          [Открыть]  [Пересчитать]│    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                                                          │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │ ЦСД-2                                ⬜ —         │    │ │
│  │  │ Расчёт не выполнен                               │    │ │
│  │  │                                        [Создать] │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  │                                                          │ │
│  │  ┌──────────────────────────────────────────────────┐    │ │
│  │  │ + Добавить секцию                                │    │ │
│  │  │                                                  │    │ │
│  │  │   Выберите тип:                                  │    │ │
│  │  │   [ЦВД ▾] [ЦСД ▾] [ЦНД ▾]                      │    │ │
│  │  │   [Контур ВД ▾] [Контур СД ▾] [Контур НД ▾]     │    │ │
│  │  └──────────────────────────────────────────────────┘    │ │
│  └──────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

### Экран 1: Поиск и выбор (Search)

*   Поисковые строки для СК и РК.
*   Карточки найденных клапанов.
*   Механизм «Предложенные связи» (если есть в БД).
*   Контекст: текущая секция отображается в заголовке.

### Экран 2: Ввод данных (Input)

```
┌──────────────────────────────────────────────────────────────┐
│  ← Dashboard          Секция: ЦВД                           │
│                                                              │
│  ═══ ОБЩИЕ ПАРАМЕТРЫ ═══                                     │
│                                                              │
│  ── Свежий пар ──                                            │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────┐ │
│  │ P_fresh          │  │ T_fresh          │  │ [T ▾ / H]  │ │
│  │ 240   [кгс/см²▾] │  │ 540     [°С ▾]   │  │            │ │
│  └──────────────────┘  └──────────────────┘  └────────────┘ │
│                                                              │
│  ── Воздух ──                          ── Последний отсос ── │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────┐ │
│  │ P_air            │  │ T_air            │  │P_lst_leak  │ │
│  │ 1.033 [кгс/см²▾] │  │ 27      [°С ▾]   │  │0.97[кгс/▾]│ │
│  │ (default)        │  │ (default)        │  │ (default)  │ │
│  └──────────────────┘  └──────────────────┘  └────────────┘ │
│                                                              │
│  ═══ ГРУППЫ КЛАПАНОВ ═══                                     │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 🔵 СК (1 шт) — 3 участка, 2 отсоса                    │  │
│  │ Клапаны: СК-1 ЦВД                                     │  │
│  │ Промежуточные отсосы: 1 поле                           │  │
│  │                                                        │  │
│  │ ┌───────────────┐                                      │  │
│  │ │ P_отсос_1     │     (Последний отсос = 0.97)         │  │
│  │ │ 10  [кгс/см²▾]│                                      │  │
│  │ └───────────────┘                                      │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 🔵 РК Тип-1 (3 шт) — 2 участка, 1 отсос              │  │
│  │ Клапаны: РК-1, РК-2, РК-3 ЦВД                        │  │
│  │ Промежуточные отсосы: нет (единственный = P_lst)       │  │
│  │                                                        │  │
│  │ ℹ️ Единственный отсос = P_lst_leak_off (0.97 кгс/см²)  │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ 🔵 РК Тип-2 (1 шт) — 4 участка, 3 отсоса             │  │
│  │ Клапаны: РК-4 ЦВД                                     │  │
│  │ Промежуточные отсосы: 2 поля                           │  │
│  │                                                        │  │
│  │ ┌───────────────┐ ┌───────────────┐                    │  │
│  │ │ P_отсос_1     │ │ P_отсос_2     │ (Последний = 0.97) │  │
│  │ │ 30  [кгс/см²▾]│ │ 5   [кгс/см²▾]│                    │  │
│  │ └───────────────┘ └───────────────┘                    │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│                               [     🔵 РАССЧИТАТЬ     ]     │
└──────────────────────────────────────────────────────────────┘
```

### Экран 3: Результаты (Output)

```
┌──────────────────────────────────────────────────────────────┐
│  ← Ввод данных          Секция: ЦВД                         │
│                                                              │
│  ═══ РЕЗУЛЬТАТЫ РАСЧЁТА ═══                                  │
│                                                              │
│  ┌─── СК-1 ЦВД (группа "СК", 1 шт) ─────────────────────┐  │
│  │ Уч. │ Z │ d,мм │  L,мм  │ δ,мм  │ P₀      │ P₂     │ G,т/ч    │  │
│  │  1  │ 2 │  40  │ 313.5  │ 0.215 │ 130.0   │ 10.00  │ 0.5366   │  │
│  │  2  │ 2 │  40  │  50.0  │ 0.215 │  10.00  │ 0.9700 │ 0.07403  │  │
│  │  3  │ 2 │  40  │  97.5  │ 0.215 │  1.033  │ 0.9700 │ 0.003496 │  │
│  │                                                        │  │
│  │ Отсосы:                                                │  │
│  │ Отсос №1: G=0.4626 т/ч, P=10 кгс/см², T=503.6°С      │  │
│  │ Отсос №2: G=0.0705 т/ч, P=0.97 кгс/см², T=478.5°С    │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─── РК Тип-1 (3 шт) ──────────────────────────────────┐  │
│  │ (детализация для 1 клапана)                            │  │
│  │ Уч. │ ... │ G,т/ч                                      │  │
│  │  1  │ ... │ 0.110                                       │  │
│  │  2  │ ... │ 0.010                                       │  │
│  │ Итого 1 клапан: 0.120 т/ч  ×  3 шт  =  0.360 т/ч     │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ═══ СВОДНЫЕ ТАБЛИЦЫ ═══                                     │
│                                                              │
│  ┌─── Σ Стопорные клапаны (СК) ──────────────────────────┐  │
│  │ Группа  │ Кол-во │ G на 1 кл. │ G суммарный           │  │
│  │ СК      │   1    │   0.6136   │    0.6136 т/ч          │  │
│  │         │        │   ИТОГО:   │    0.6136 т/ч          │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  ┌─── Σ Регулирующие клапаны (РК) ──────────────────────┐  │
│  │ Группа    │ Кол-во │ G на 1 кл. │ G суммарный         │  │
│  │ РК Тип-1  │   3    │   0.120    │    0.360 т/ч         │  │
│  │ РК Тип-2  │   1    │   0.095    │    0.095 т/ч         │  │
│  │           │        │   ИТОГО:   │    0.455 т/ч          │  │
│  └────────────────────────────────────────────────────────┘  │
│                                                              │
│  [ ← Пересчитать ]         [ В GitLab ]    [ В Excel ]      │
└──────────────────────────────────────────────────────────────┘
```

---

## Раздел 8. Нефункциональные требования

| Требование | Значение | Комментарий |
|------------|----------|-------------|
| Производительность расчёта | < 0.2 сек | На одну группу |
| Точность | Не хуже 0.1% | Сверка с эталонной методикой |
| UX | Отсутствие «паразитных» кликов | Переходы по `Tab` между полями ввода |
| Локализация | Русский язык | Интерфейс и отчёты |
| Макс. клапанов в расчёте | **10** типов | Количество в группе не ограничено |
| Макс. секций в проекте | **10** | Покрывает 5 цилиндров × 2 |

---

## Раздел 9. Открытые вопросы

| # | Вопрос | Кто должен ответить | Срок |
|---|--------|---------------------|------|
| 1 | Формат и источник миграции текущей БД геометрии? | Архитектор / Заказчик | TBD |
| 2 | Структура JSON файла результатов (финальная валидация)? | Lead Developer | TBD |
| 3 | Связи СК↔РК: нужно ли проектировать таблицу связей или достаточно ручного выбора? | Заказчик | TBD |
| 4 | `sectionId` — генерировать автоматически (UUID) или использовать код типа + номер (`CSD-1`)? | Lead Developer | TBD |
| 5 | Нужна ли модель `Project` в БД PostgreSQL или проект существует только в контексте Task Manager / GitLab? | Архитектор | TBD |
| 6 | Auto-numbering: при добавлении второго ЦСД — первый переименовывается ЦСД → ЦСД-1, или сразу первый создаётся как ЦСД-1? | UX / Заказчик | TBD |

---

## Раздел 10. Согласования

| Роль | ФИО | Подпись/дата |
|------|-----|--------------|
| Заказчик | Деминов А.М. | |
| Аналитик | Шлёгин Л.Р., Гаврилов П.Я. | |

---

## Definition of Ready (обновлено)

Задача считается выполненной и готовой к приёмке, если:

1. **Согласование:** Спецификация v2.4 утверждена заказчиком.
2. **Данные:** БД геометрии клапанов спроектирована и наполнена минимальным набором тестовых данных (как минимум 1 тип СК и 2 типа РК).
3. **Секции:** Реализован Dashboard секций с возможностью создания, открытия и пересчёта. Работает auto-numbering (BR-21).
4. **Логика:** Реализован алгоритм «Авто-топологии» (BR-01, BR-02), корректно определяющий количество камер и распределяющий давления с учётом глобального `P_lst_leak_off`.
5. **Группировка:** Клапаны автоматически группируются по ключу геометрии (BR-12). Ввод параметров — один раз на группу.
6. **Физика:** Подключена библиотека IAPWS-97 для точного расчёта.
7. **Отчётность:** Экспорт в Excel с детальной разбивкой и сводными таблицами (BR-05).
8. **Интеграция GitLab:** Реализовано сохранение `result.json` в директорию секции и обновление `manifest.json`. Гидрация через manifest → dashboard → result.json.
9. **Дефолты:** Поля `P_air`, `T_air`, `P_lst_leak_off` имеют значения по умолчанию (BR-22) и редактируемы.

---

## Визуализация полного flow

```
┌─────────────────────────────────────────────────────────────┐
│                    TASK MANAGER                              │
│                  (parent window)                             │
│                        │                                     │
│                   postMessage                                │
│                  {projectId}                                 │
│                        ▼                                     │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              VALVE STEMS (iframe)                    │   │
│  │                                                      │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ STEP 0: Dashboard секций                       │  │   │
│  │  │ (manifest.json → список секций)                │  │   │
│  │  │                                                │  │   │
│  │  │ [ЦВД ✅] [ЦСД-1 ✅] [ЦСД-2 ⬜] [+ Добавить]   │  │   │
│  │  └──────────────────┬─────────────────────────────┘  │   │
│  │                     │ выбор / создание секции         │   │
│  │                     ▼                                 │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ STEP 1: Выбор клапанов для секции              │  │   │
│  │  │ (или гидрация из result.json секции)           │  │   │
│  │  └──────────────────┬─────────────────────────────┘  │   │
│  │                     ▼                                 │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ STEP 2: Ввод параметров                        │  │   │
│  │  │ Глобальные: P/T свеж.пара + P/T воздух + P_lst │  │   │
│  │  │ По группам: промежуточные отсосы               │  │   │
│  │  └──────────────────┬─────────────────────────────┘  │   │
│  │                     ▼                                 │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ STEP 3: Результаты                             │  │   │
│  │  │ Детализация + Сводные + Отсосы                 │  │   │
│  │  │ [← Пересчитать] [В GitLab] [В Excel]          │  │   │
│  │  └──────────────────┬─────────────────────────────┘  │   │
│  │                     │ "В GitLab"                      │   │
│  │                     ▼                                 │   │
│  │  ┌────────────────────────────────────────────────┐  │   │
│  │  │ SAVE:                                          │  │   │
│  │  │ → valve-stems/{sectionId}/result.json          │  │   │
│  │  │ → valve-stems/manifest.json (update)           │  │   │
│  │  │ → Возврат на Dashboard секций                  │  │   │
│  │  └────────────────────────────────────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```
